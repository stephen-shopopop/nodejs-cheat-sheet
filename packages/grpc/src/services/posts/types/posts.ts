export interface Posts {
  /** Posts id */
  id?: (string|null)

  /** Posts title */
  title?: (string|null)

  /** Posts body */
  body?: (string|null)

  /** Posts category */
  category?: (string|null)
}
